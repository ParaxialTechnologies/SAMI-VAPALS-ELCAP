This project uses a git submodule for the docs/ directory, which is why it
doesn't seem like it works the same way as before.

You should use the following commands to help configure this properly:

git config push.recurseSubmodules check

